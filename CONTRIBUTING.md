# راهنمای مشارکت در Fast Rub

از علاقه‌مندی شما به مشارکت در توسعه Fast Rub سپاسگزاریم. لطفاً پیش از شروع، موارد زیر را مطالعه فرمایید.

---

## مراحل مشارکت

۱. مخزن را Fork کنید.

۲. آن را روی سیستم خود کلون کنید:

```bash
git clone https://github.com/YOUR_USERNAME/fast_rub.git
cd fast_rub
```

۳. یک شاخه جدید ایجاد کنید. نام شاخه را بر اساس نوع تغییر انتخاب کنید:

```bash
git checkout -b fix/antispam-bug
git checkout -b feature/cache-freeze
git checkout -b docs/update-readme
```

۴. تغییرات خود را اعمال کنید.

۵. تغییرات را ثبت و ارسال کنید:

```bash
git add .
git commit -m "توضیح مختصر تغییرات"
git push origin fix/antispam-bug
```

۶. یک Pull Request به شاخه `main` ارسال کنید.

---

## هماهنگی پیش از شروع

پیش از آغاز کار روی هر قابلیت جدید یا تغییر قابل توجه، لطفاً از طریق تلگرام با نگه‌دارنده پروژه هماهنگ کنید. این هماهنگی به دلایل زیر ضروری است:

- احتمال دارد قابلیت مشابهی در دست توسعه باشد.
- ممکن است تصمیمات معماری خاصی اتخاذ شده باشد که از آن مطلع نباشید.
- از اتلاف وقت و تلاش شما جلوگیری می‌شود.

برای هماهنگی کافی است پیام کوتاهی ارسال کنید و شرح دهید چه تغییری در نظر دارید.

**تلگرام:** [@O_and_ONE_01](https://t.me/O_and_ONE_01)

---

## اولویت‌های پروژه

مهم‌ترین حوزه‌هایی که می‌توانید در آن‌ها مشارکت کنید:

۱. **نوشتن تست خودکار** — در حال حاضر پروژه فاقد تست خودکار است و این مهم‌ترین کمبود محسوب می‌شود.

۲. **رفع باگ و دیباگ** — شناسایی و اصلاح مشکلات موجود.

۳. **بهبود مستندات** — تکمیل یا شفاف‌سازی بخش‌های ناقص.

۴. **افزودن قابلیت جدید** — با هماهنگی قبلی.

---

## استایل کد

از استاندارد `PEP 8` پیروی کنید:

- نام متغیرها: `snake_case`
- نام کلاس‌ها: `PascalCase`
- نام توابع: `snake_case`
- تو رفتگی: ۴ فاصله (بدون tab)

کد باید خوانا و قابل فهم باشد. کدی که برای دیگران قابل درک نباشد، قابل بررسی نخواهد بود.

کامنت‌ها بهتر است انگلیسی باشند.

---

## قراردادهای پروژه

چند نکته که مخصوص Fast Rub است:

- تمام توابع به صورت async پیاده‌سازی می‌شوند. از `async def` استفاده کنید.
- برای اجرای توابع از `Utils.run_handler` استفاده کنید.
- برای ارسال پیام، از Client استفاده کنید تا WaitManager رعایت شود.
- خطاها را با exceptionهای تعریف‌شده در Fast Rub پرتاب کنید، نه `Exception` خام.

---

## تست

در حال حاضر تست خودکار وجود ندارد. در صورت تمایل به راه‌اندازی، پیشنهاد می‌شود از موارد زیر استفاده کنید:

- `pytest` برای unit test
- `pytest-asyncio` برای تست توابع async
- مسیر پیشنهادی: `tests/`

---

## ارسال Pull Request

در توضیحات PR موارد زیر را ذکر کنید:

- چه تغییری اعمال شده است.
- دلیل این تغییر چیست.
- چگونه آن را تست کرده‌اید.

از ترکیب چند قابلیت در یک PR خودداری کنید. هر PR باید به یک موضوع مشخص بپردازد.

---

## گزارش باگ

در issue موارد زیر را ذکر کنید:

- مراحلی که منجر به بروز باگ شده است.
- رفتار فعلی برنامه.
- رفتار مورد انتظار.
- نسخه پایتون.
- سیستم‌عامل.

در صورت امکان، نمونه کد ساده‌ای که باگ را بازتولید می‌کند ضمیمه کنید.

---

## قواعد رفتاری

- به سایر مشارکت‌کنندگان احترام بگذارید.
- از تمسخر کد دیگران خودداری کنید.
- در صورت عدم آگاهی، پرسش کنید.
- از تمجید غیرصادقانه پرهیز کنید.
- صریح و در عین حال محترمانه باشید.

---

## لایسنس

Fast Rub تحت لایسنس MIT منتشر شده است. با مشارکت در این پروژه، موافقت می‌کنید که کد شما نیز تحت همین لایسنس منتشر شود.

---

## تماس

در صورت وجود هرگونه پرسش:

**تلگرام:** [@O_and_ONE_01](https://t.me/O_and_ONE_01)
